& "../lib/http.noe";

@main() -> i64 {
    ? (arg_count() < 2) {
        ! "usage: http_get <url>";
        ^ 1;
    }

    res := http_get(arg(1));
    ! "status=" + text_of(res.status);
    ! res.body;
    ^ 0;
}
